export enum MessageErrorResource {
    msg_password_pattern = 'Your password must contain at least 3 out of 4 following rules, lowercase, uppercase, numerals, special characters such as ! @ # $ % ^ & * ( ) { } [ ]'
}