import { LoginResource } from '../../enums/LoginResource';

import { Component, OnInit } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'login-form',
    templateUrl: 'login.component.html'
})

export class LoginFormComponent implements OnInit {
    // ngOnInit
}
